import React from 'react';
import { Typography, Box } from '@mui/material';

function MatrixView() {
  return (
    <Box>
      <Typography variant="h4" component="h1" gutterBottom>
        マトリックス表示
      </Typography>
      <Typography variant="body1">
        アイゼンハワーマトリックス画面の実装予定
      </Typography>
    </Box>
  );
}

export default MatrixView;